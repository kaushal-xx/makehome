<%= form_with(model: slider, local: true) do |form| %>
  <% if slider.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(slider.errors.count, "error") %> prohibited this slider from being saved:</h2>

      <ul>
        <% slider.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :slider_type %>
    <%= form.text_field :slider_type %>
  </div>

  <div class="field">
    <%= form.label :visibility %>
    <%= form.check_box :visibility %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
